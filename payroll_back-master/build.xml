<project name="payroll_back-master" default="build" basedir=".">
    
    <!-- Define properties -->
    <property name="src.dir" value="src/java"/>
    <property name="web.dir" value="src/main/webapp"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="war.file" value="my-java-ee-project.war"/>
    <property name="lib.dir" value="lib"/>

    <!-- Define classpath -->
    <path id="classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
        <!-- Add Servlet API JAR to the classpath -->
        <pathelement location="${lib.dir}/servlet-api.jar"/>
    </path>

    <!-- Define targets -->
    <target name="init">
        <mkdir dir="${build.dir}"/>
        <mkdir dir="${classes.dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath" includeantruntime="false">
            <!-- Exclude JWTTokenGenerator.java and any other files that depend on io.jsonwebtoken package -->
            <exclude name="util/JWTTokenGenerator.java"/>
            <!-- Add more exclude elements for other files if needed -->
        </javac>
    </target>

    <target name="war" depends="compile">
        <war destfile="${war.file}" webxml="${web.dir}/WEB-INF/web.xml">
            <fileset dir="${web.dir}"/>
            <classes dir="${classes.dir}"/>
        </war>
    </target>

    <target name="build" depends="war"/>

</project>
